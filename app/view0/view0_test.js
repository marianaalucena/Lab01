'use strict';

describe('myApp.view0 module', function() {

  beforeEach(module('myApp.view0'));

  describe('view0 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view0Ctrl = $controller('View0Ctrl');
      expect(view0Ctrl).toBeDefined();
    }));

  });
});